UNDEFINED===FALSE!;
null==false
!0=true;
isNaN()=Проверяет число ли это значение или нет!; если число то фалсе если слово то тру
Number.isInfinite()= проверяет число это или нет если число то тру если сторка фалс; Строгая проверка на типы данных
isInfinite()=проверяет число это или нет если число тру, приводит строку "300" к типу данных число.
условие лоижть в переменную!
object || arr.toString() преобразует в строку

Math.floor(Math.random() * (max - min)) + min;

array=[money]

in по индексу тип array[0]=money;

of по значению =money

elem.scrollTop = это скрытое расстояние от вверха родителя до вверха экрана который мы видим!
scroll = так же даёт знать куда переместиться если например top(200) || top =200 мы переместимся на 200 пикселей от вверха
elem.offset получает значение где находится элемент относительно боди!

elem.offsetwidth || offsethieght = значение родителя в ширине или высоте

splice удаляет из массива и возращает массив с удаленными айтемами (индексмассива, Количесвто айтемов для удаления,(можно добавить элементы который добавится за место удаленных элемнентов))

slice не удаляет из массива и создает новый массив из массива (индексмассива, индекс последующего айтема до которго нужно дойти)
Object.keys() = создает массив из ключей обьекта

Object.Values()= создает массив из значений ключей обьекта

Здравствуйте, меня зовут Евгений, сейчас нахожусь на курсах по React+Node.js.
HTML/CSS/JS
Предлагаю вам верстку.
Верстаю лендинги, адаптивные промо-сайты, корпоративные порталы, доделываю, подправляю элементы страничек.
Так же верстаю максимально кроссбраузерно, валидно (W3C) HTML 5, аккуратно и продуманно для дальнейшей передачи.

В работе использую SASS компилятор.
По надобности фреймворки, плагины по мере необходимости. 
Если нужен Bootstrap(4 версии) без проблем, но предпочтительно верстаю без него.
придерживаюсь поддержки браузеров; (IE10+, FF, Opera, Safari, Chrome).
Максимум CSS3, Адаптив, Pixel-Perfect от 95%,
Для проверки можете дать тестовую верстку или что-то подобное.

Каждые 5 часов или когда вам понадобиться буду скидывать результат.

При дальнейшем сотрудничестве проекты будут выполняться в том же стиле, или стиле который будет нужен.
Проекты простой и средней сложности от часа до 3 дней.
Стоимость моей работы в 300 рублей в час. (можно обсудить цены)

Заинтересован в постоянном сотрудничестве, и регулярных заказах.


имяпеременной.replace(изначальный символ, на символ который хотим изменить)-replace заменяет что надо!

имя переменной = Object.assign(обьект)-копирует свойства или ключи одного в обьект в дргоуй

array.reverse=работает только на массивы разоврачивает массив


$()=>{} jquery dom-ready вместо записи document.ready ....

родитель.siblings или дочерний элемент.siblings() покажет рядом лежащие дочерние элементы


для написания комита можно использовать:

Если применяется, этот коммит будет ... сообщение комита

курс ноонторопа максимум 2,5 недели,=== отдыху так же столько времени

document.addEventListener('DOMContentLoaded', (event) => {
    
})

while- в то время как ---- перевод.

Нужно называть переменные так чтобы отвечало на вопрос что делает?

sass --watch style.scss:../css/style.css использование sass

IIFE = (()=>{})() функция вызванная на месте

apply и call, bind связывают this в функции

function(argument1,argument2) можно посмотреть через [arguments] в функции

new Set() это обьект который принимает только уникальные значение

+ самое быстрое преобразование в строку

isArray()== проверят массив это или нет

проверить есть ли свойство в обьекте имясвойства in обьект разница между  hasOwnProperty в том что in может искать в прототипах обьекта а has own нет

amazing_man10 yevgeniy-ten jetbraincs account

throw - останавливает дальнейшее выполнение и сразу передается в catch 
лучше использовать throw new Error для кросбраузерности это автоматом обьекты какие то там в стринг переводит

firebase создать проект
перед firebase init надо подготовить проект к продакшену там минифицировать и тому подобное
выбираем нужные параметры например хостинг, api и тд
потом выбираем в какой проект помещать, и какая у нас публичная директория там само спросит
потом в конце firebase deploy если чето изменили опять deploy делаем

e.target.classList.contains("имя класса") можно проверить на наличие класса

Для удаления из массива определенного айтема

Array.includes(element) проверяет есть ли этот элемент в массиве если есть тру
Array.prototype.remove = function(from, to) {
  var rest = this.slice((to || from) + 1 || this.length);
  this.length = from < 0 ? this.length + from : from;
  return this.push.apply(this, rest);
};

openServer прога может запустить хостинг 

Prototype методы обьектов можно создать
Object.prototype.метод

arr.indexOf() примерно как find Index

REACT_APP_(имя скррытой переменной) = данные
process.env.REACT_APP_(имя)

useMemo() === это как componentDiDUpdate() то есть если прошлые значение от текущих значений не изменились, то не нужно выполнять ту или иную функцию

 // Object.entries создаёт массив из массивов [key,value] обратно fromEntries
